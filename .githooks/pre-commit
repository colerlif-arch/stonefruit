#!/bin/sh
set -eu

if command -v py >/dev/null 2>&1; then
  py -3 scripts/check_secrets.py
elif command -v python >/dev/null 2>&1; then
  python scripts/check_secrets.py
else
  echo "Python is required for secret scanning hook."
  exit 1
fi
