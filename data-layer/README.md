# Data Layer

This layer owns ingestion from external and local data sources.

- `ingestors/`: source-specific connectors and persistence logic.
- `raw/`: raw persisted data for downstream derivation.
