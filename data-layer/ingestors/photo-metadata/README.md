# Photo Metadata Ingestor (Planned)

Planned responsibilities:
- Extract EXIF and file metadata from local photo stores.
- Persist immutable photo facts (capture time, geo, camera, hash).
- Keep content-addressable references to avoid duplicate ingestion.
